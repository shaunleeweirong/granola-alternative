<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Recording the microphone. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Electron runs a JIT, which the hardened runtime blocks by default. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- The app loads a native database module and spawns its own helper
         binaries (the audio tap and the speech engine). Library validation
         would reject them unless every one carries the same team signature. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
